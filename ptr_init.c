#include <stdio.h>

int main()
{
    // 指针变量的初始化
    // short int* ptr;
    // short int num;

    // ptr = &num;
    // *ptr = 89;

    int* blankptr = NULL;
    printf("%s \n",*blankptr);
    printf("%s \n",blankptr);

    // printf("ptr is %d;\nnum is %d\n",*ptr,num);
    
}